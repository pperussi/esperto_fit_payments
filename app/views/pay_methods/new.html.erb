<% if @method_pay.errors.any? %>
  <%  @method_pay.errors.full_messages.each do |msg| %>
    <%= msg %>
  <%end %> 
<%end %> 

<%=form_for @method_pay do |m| %>
    <%= m.label :name, 'Metodo de pagamento' %>
    <%= m.text_field :name %>
    <%= m.label :tax, 'Taxa cobrada pela empresa' %>
    <%= m.number_field :tax %>
    <%= m.label :limit_days, 'Limite de dias para a cobrança' %>
    <%= m.number_field :limit_days %>
    <%= m.submit 'Cadastrar' %>
<%end%>