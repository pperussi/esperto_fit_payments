<%= form_with(url: "/clients/search", method: "get") do %>
  <%= label_tag(:q, "Pesquisar aluno") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Buscar") %>
<% end %>

<%unless @client.blank?%>
  <% @client.each do |f| %>
  <%= link_to f.name, single_class_path(f) %>
  <%end%> 
<%end%> 